/** Page structure. Every route is assembled from these. */
export { AppShell, type AppShellProps, NavLink, type NavLinkProps, navLinkClass } from "./AppShell";
export {
  PageBody,
  type PageBodyProps,
  PageHeader,
  type PageHeaderProps,
  PageSection,
  type PageSectionProps,
} from "./Page";
