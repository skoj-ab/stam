# Agent guidance

Follow `CONTRIBUTING.md` and the documentation under `docs/`. Read
`docs/design-system.md` before changing frontend code.

Never commit credentials, databases, exports, or real personal data. Before
finishing a change, run `bun test`, `bun run typecheck`, `bun run lint`, and
`bun run build`.
